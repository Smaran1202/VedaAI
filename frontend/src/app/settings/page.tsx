import { UtilityPage } from "@/components/utility-page";

export default function SettingsPage() {
  return (
    <UtilityPage
      title="Settings"
      description="Workspace settings are outside this assessment scope. The demo uses local environment variables for API, database, Redis, and AI configuration."
    />
  );
}
